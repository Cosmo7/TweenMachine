

This demo scene uses Unity's built-in render pipeline and the old-school input system.

If you're having difficulty loading it inside your existing project it's probably easier just to 
create a new project using the default 3d options to run it.

All of the juicy example code is in ExampleController.cs